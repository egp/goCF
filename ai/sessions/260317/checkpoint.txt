Project: goCF, a mathematically correct GCF-first continued-fraction arithmetic library.
Current milestone: rebuild sqrt from scratch around one public API, `SqrtGCF(GCFSource)`.
Current sqrt path is new/bootstrap, not legacy:
- lazy construction works
- exact finite squares work
- exact finite rational squares work
- exact finite non-squares use bounded Newton rational fallback
- metadata fast path works for square radicands (e.g. sqrt(4)) without reading source terms
- `AdaptCFToGCF` now preserves `QuadraticRadicalSource` semantics
New sqrt stream has:
- bounded per-call ingestion
- persistent buffered prefix state
- recorded terminal errors
What is broken / incomplete:
- unresolved long-input state is still being treated as an error in current prod
- need-more-input must be live (`Err()==nil`) until true terminal failure
Current failing tests before handoff targeted exactly that:
- unresolved first Next should be live not error
- second Next should continue from prior progress without error
- eventual bootstrap-budget exhaustion should become the first real error
Immediate next step:
- apply `sqrt_gcf.go v12` style change:
  - unresolved after bounded work returns no term but keeps stream live
  - `Err()` reserved for terminal conditions only
Constraints/invariants:
- only new public sqrt API: `SqrtGCF(src GCFSource) (ContinuedFraction, error)`
- input model is GCF-only; output is regular CF terms
- keep budgets internal for now
- do not assume whole-input value ahead of time
- preserve lazy ingestion and bounded work per `Next()`
- newly pasted files/errors are source of truth; discard obsolete earlier versions