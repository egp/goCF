Project: goCF — mathematically correct, testable continued-fraction / generalized-continued-fraction arithmetic with GCF-ingesting operator paths.

Current milestone:
Retire remaining MVP special seams and keep operator-facing paths uniformly GCF-shaped.

Working components:
- e is ingested as GCF via MVPEGCFSource / NewECFGSource
- sqrt(5) is ingested as GCF via AdaptCFToGCF(Sqrt5CF())
- 69° is represented as an exact finite GCF source, not tail-special handling
- denominator no longer uses exact-tail trig entry
- numerator live path no longer depends on fake finite bridge source
- numerator radicand is assembled through explicit snapshot helpers
- explicit exact scalar snapshots now exist for numerator scale-factor construction
- target outputs currently support certified range, midpoint rational, and midpoint RCF terms

Known failures:
- none currently; tests are green

Next immediate task:
Decide whether to stop scalar cleanup at exact point snapshots for MVP, or add a tiny exact-finite-GCF scalar source helper and route scalar snapshots through that helper.

Constraints / invariants:
- stay on the critical path; no float work
- prefer test-first: production stubs, failing tests, then implementation
- finite GCFs are treated as a subset of GCF; ingesters should work by ingestion, not tail special cases
- remove tail-era concepts and naming where practical
- e should remain GCF-ingested
- prefer full replacement files or contiguous complete replacement functions over partial block edits
- do not speculate about APIs; work from pasted current source